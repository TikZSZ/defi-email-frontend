import { BaseMirrorClient, Params } from "./";
declare class AxiosClient implements BaseMirrorClient {
    baseURL: string;
    private baseApi;
    constructor(baseURL: string);
    fetch<D = any>(url: string, params: Params): Promise<D>;
}
export { AxiosClient as Client };
