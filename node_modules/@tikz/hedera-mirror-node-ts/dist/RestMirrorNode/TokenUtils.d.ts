import { BaseMirrorClient } from ".";
import { TokenBalances, Tokens, TokenInfo } from "./tokenBaseClasses";
export declare class TokenUtils {
    private mirrorClient;
    private url;
    constructor(mirrorClient: BaseMirrorClient, url: string);
    static v1(mirrorClient: BaseMirrorClient): TokenUtils;
    get Tokens(): Tokens;
    get TokenBalance(): TokenBalances;
    get TokenInfo(): TokenInfo;
}
