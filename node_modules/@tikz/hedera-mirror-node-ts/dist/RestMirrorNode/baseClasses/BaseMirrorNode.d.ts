import { BaseMirrorClient } from "./BaseMirrorClient";
export declare abstract class BaseMirrorNode<D> {
    protected mirrorClient: BaseMirrorClient;
    protected abstract url: string;
    constructor(mirrorClient: BaseMirrorClient);
    protected fetch: (url: string) => Promise<D>;
    abstract get(): Promise<D>;
}
