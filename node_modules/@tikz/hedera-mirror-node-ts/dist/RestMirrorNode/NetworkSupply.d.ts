import { BaseMirrorClient, BaseMirrorNode } from "./";
export declare class NetworkSupply extends BaseMirrorNode<NetworkSupplyResponse> {
    protected url: string;
    constructor(mirrorNodeClient: BaseMirrorClient, url: string);
    static v1(mirrorNodeClient: BaseMirrorClient): NetworkSupply;
    protected params: {};
    get(): Promise<NetworkSupplyResponse>;
}
export interface NetworkSupplyResponse {
    released_supply: string;
    timestamp: string;
    total_supply: string;
}
